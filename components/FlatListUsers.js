import { Text, StyleSheet } from "react-native";

export function FlatListUsers() {
  return <Text style={styles.writtenContent}>FlatListUsers Component</Text>;
}

const styles = StyleSheet.create({
  writtenContent: {
    color: "white",
  },
});
